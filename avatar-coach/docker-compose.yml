services:
  postgres:
    image: postgres:16
    container_name: avatar-coach-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: avatar_coach
      POSTGRES_USER: avatar
      POSTGRES_PASSWORD: avatar1234
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: