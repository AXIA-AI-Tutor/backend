spring:
  application:
    name: avatar-coach

  datasource:
    url: jdbc:postgresql://localhost:5432/avatar_coach
    username: avatar
    password: avatar1234
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true